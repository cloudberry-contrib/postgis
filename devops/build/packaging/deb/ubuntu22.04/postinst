#!/bin/bash

set -e

# The package ships the custom GEOS/PROJ/GDAL/SFCGAL runtime libraries under
# /usr/local/... together with an /etc/ld.so.conf.d entry. Refresh the dynamic
# linker cache so the PostGIS extension can resolve them.
if [ "$1" = configure ]; then
  ldconfig
fi

exit 0
